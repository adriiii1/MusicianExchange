<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.adri.musicianexchange">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Design"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".Main2Activity"
                android:label="@string/title_activity_main2"
                android:theme="@style/Theme.Design.NoActionBar">
        </activity>
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".signupActivity"></activity>
        <activity android:name=".nuevoAnuncioVenta"></activity>
        <activity android:name=".listasActivity"></activity>
        <activity android:name=".conciertosActivity"></activity>
        <activity android:name=".resetPasswordActivity"></activity>
        <activity android:name=".gruposActivity"></activity>
        <activity android:name=".nuevoAnuncioGrupo"></activity>
        <activity android:name=".ventasActivity"></activity>
    </application>

</manifest>